# 요건 카드

요건 ID: REQ-014
제목: 할 일 카테고리 관리 화면
우선순위: 중간
상태: 대체됨
요건 종류: 기능
명세 역할: 구현 슬라이스
대상 시스템: application
제품 영역: category
품질 속성: none
검증 수준: static
관련 요건: REQ-015
대체 요건: REQ-015

## 사용자/목적

이 카드는 기존 카테고리 관리의 화면 구현 슬라이스였다. 현재 표준에서는 API 카드와 화면 카드처럼 구현 표면 때문에 분리된 카드를 장기 명세 원천으로 유지하지 않는다.

## 범위

- 본 카드는 더 이상 활성 명세 원천이 아니다.
- 카테고리 화면의 목록, 생성, 수정, 삭제, 보호 라우트, 접근성 검증은 REQ-016~REQ-019의 해당 원자 요건으로 이동한다.

## 표준 용어

- category.category
- ui.appShell

## 제외 범위

- 신규 화면 동작 추가
- REQ-016~REQ-019 화면 AC 중복 관리

## 수용 기준

- (STATIC) 할 일 카테고리 관리 화면 명세는 REQ-015 카테고리 관리로 대체된다

## 의사결정 로그

- 결정일: 2026-06-02
  결정: 기존 REQ-014 화면 슬라이스는 REQ-015 카테고리 관리 묶음으로 대체하고, 상세 화면 AC는 REQ-016~REQ-019로 이동한다.
  이유: 카테고리 API와 화면은 같은 사용자 능력을 검증하는 서로 다른 구현 표면이므로 별도 REQ로 장기 유지하지 않는다.
  결정자: Product Owner, Tech Lead
  영향: 기존 REQ-014의 Scenario, FE BDD Test, Storybook metadata, 화면 annotation은 새 원자 요건으로 이동한다. 본 카드는 `대체됨` 상태로 추적에서 INACTIVE가 된다.

## 수용 테스트 리뷰

- 대체 카드로 종료한다. 실행 검증은 REQ-016~REQ-019에서 관리한다.

## 열린 질문

- 없음
