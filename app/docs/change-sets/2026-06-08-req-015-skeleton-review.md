# Change Set: 2026-06-08 REQ-015 Skeleton 검토 전환

상태: 완료
요청일: 2026-06-08
변경 유형: 마이그레이션, 수정
영향 요건: REQ-015
논의 상태: 없음

## 요청 요약

- 단계 인식 TDD 워크플로우에 맞춰 `REQ-015` 카테고리 관리 상위 요건을 한 단계씩 검증하며 이관한다.
- 첫 단계로 레거시 `검토중` 상태를 `Skeleton 검토중`으로 전환하고 상위 요건의 E2E 검증 표면을 명시한다.

## 작업 범위

- `REQ-015` 상태를 `Skeleton 검토중`으로 변경한다.
- `REQ-015`에 `검증 대상` 섹션을 추가한다.
- 상위 요건은 하위 원자 요건의 API/DB/UI/Storybook 계약을 복사하지 않고, 실 백엔드 + 실 프런트엔드 Playwright 통합 스모크로 결합 성과를 검토함을 명시한다.
- 기존 AC, Scenario Covers, 실행 테스트 문자열은 변경하지 않는다.

## 제외 범위

- `REQ-015`의 `Skeleton 승인` 승격.
- 하위 원자 요건 `REQ-016`~`REQ-020`의 상태 변경.
- 카테고리 API, DB, UI, Storybook 계약 변경.
- 애플리케이션 업무 코드와 테스트 코드 변경.

## 완료 조건

- `REQ-015` 단일 카드 trace가 RED 없이 통과한다.
- 앱 Change Set 리포트에 경고가 없어야 한다.

## 검증 명령

- `npm run app:trace -- --requirement REQ-015`
- `npm run app:trace`

## 결정 로그

- 2026-06-08: `REQ-015`는 카테고리 관리 묶음의 상위 요건이므로 직접 검증 대상은 E2E로 한정하고, API/DB/UI/Storybook 상세 계약은 `REQ-016`~`REQ-020`이 소유한다.

## 열린 논의

- 없음
