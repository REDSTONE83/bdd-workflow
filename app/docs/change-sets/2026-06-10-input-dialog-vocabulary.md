# Change Set: 2026-06-10 입력 대화상자 어휘 적용

상태: 완료
요청일: 2026-06-10
변경 유형: 마이그레이션
영향 요건: REQ-017, REQ-018, REQ-022, REQ-024
논의 상태: 없음

## 요청 요약

- 생성·수정 입력 팝업 UI의 정규 명칭을 `입력 대화상자`로 개정한 하네스 UI 어휘 표준에 맞춰 앱 표준, 현재 Storybook 설명, 활성 요건 카드 문장을 정리한다.

## 작업 범위

- FE UI/테스트 표준의 기존 생성·수정 입력 UI 명칭을 `입력 대화상자` 중심으로 바꾼다.
- 카테고리와 할 일 경로 화면 Storybook 설명의 사용자 흐름에서 생성 입력 UI를 `입력 대화상자`로 표현한다.
- 카테고리 생성/수정 컴포넌트 주석과 생성/수정 Storybook component 설명을 새 정규 명칭에 맞춘다.
- REQ-017/REQ-018/REQ-022/REQ-024 카드의 범위, Skeleton, 의사결정 로그, 리뷰 문장에 남은 기존 입력 UI 명칭을 `입력 대화상자`로 이관한다.

## 제외 범위

- 대화상자 컴포넌트 API, 파일명, export 이름 변경.

## 완료 조건

- 앱 표준, 현재 Storybook 설명, 활성 요건 카드에서 생성·수정 입력 팝업 UI는 `입력 대화상자`로 표현된다.
- 확인 목적 UI는 `확인 대화상자` 표현을 유지한다.
- Storybook 빌드와 프런트엔드 source-index가 통과한다.

## 검증 명령

- `npm run build-storybook`
- `npm run source-index`

## 결정 로그

- 2026-06-10: 앱 표준과 Storybook 설명은 신규 정규 명칭인 `입력 대화상자`를 우선 사용한다.
- 2026-06-10: 활성 요건 카드의 범위, Skeleton, 의사결정 로그, 리뷰 문장도 `입력 대화상자`로 이관한다. 해당 표현은 추적용 AC/Covers 핵심 문장이 아니므로 FE BDD `Covers` 변경은 필요하지 않다.

## 열린 논의

- 없음
