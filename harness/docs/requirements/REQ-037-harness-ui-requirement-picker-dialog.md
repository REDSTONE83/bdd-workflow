# 요건 카드

요건 ID: REQ-037
제목: 하네스 UI 요건 검색/선택 대화상자
우선순위: 중간
상태: Skeleton 검토중
요건 종류: 하네스
명세 역할: 원자 요건
대상 시스템: harness
제품 영역: harness
품질 속성: usability
검증 수준: acceptance
관련 요건: REQ-031, REQ-034, REQ-035
대체 요건: 없음

## 사용자/목적

하네스 작업자는 요건 ID를 정확히 기억하지 않아도, 여러 하네스 화면에서 요건 ID와 제목, 상태 정보를 검색해 단일 요건을 선택하거나 선택 해제할 수 있어야 한다.

## 범위

- 요건 검색/선택 대화상자는 호출 화면이 전달한 후보 요건 목록을 목록형 카드로 표시한다.
- 후보 요건 카드는 요건 ID와 제목을 한 줄로 표시하고, 추적 상태 뱃지는 우측 끝에 배치하며, 명세 역할, 상위 요건 ID, 하위 요건 수만 함께 표시해 목록 밀도를 유지한다.
- 하위 후보 요건 카드는 좌측 들여쓰기와 세로선으로 하위 관계를 시각적으로 구분한다.
- 검색은 요건 ID, 제목, 추적 상태, 카드 상태, 제품 영역, 우선순위, 명세 역할, 상위 요건 ID를 대상으로 한다.
- 상위 요건이 검색어와 직접 일치하면 직계 하위 후보 요건도 목록에 표시하고, 상위 검색 때문에 포함된 후보는 직접 매칭 후보와 구분한다.
- 대화상자는 별도 현재 선택 요약 영역을 두지 않고, 후보 목록 안의 선택된 요건만 강조하며 검색 입력의 작은 해제 버튼으로 선택 해제를 지원한다.
- 대화상자 높이는 검색 결과 수와 무관하게 viewport 기준 고정값을 유지하고, 제목과 검색 입력은 유지한 채 후보 목록 영역만 상하 스크롤한다.
- 검색 결과가 없으면 빈 결과 안내를 표시한다.
- 호출 화면은 대화상자가 반환한 선택 요건을 명령 인자, Change Set 필터 같은 자기 화면의 의미로 적용한다.
- Storybook Docs에서는 대화상자가 문서 본문을 가리지 않도록 닫힌 호출 화면 프레임으로 렌더링하고, Canvas에서 열린 대화상자 상태를 검토한다.

## 표준 용어

- harness.requirementCard
- harness.traceState
- harness.scope

## 제외 범위

- 요건 생성·수정·삭제. 조회와 선택 전용이다.
- 선택된 요건을 명령 인자나 Change Set 필터에 적용하는 부모 화면 정책. 각 호출 화면 요건이 다룬다.
- 요건 상세 화면으로 이동하는 링크. 목록 또는 부모 화면의 별도 행위로 다룬다.

## 수용 기준

- (UI) 요건 검색/선택 대화상자는 후보 요건 목록을 요건 ID와 제목이 한 줄에 있고 추적 상태 뱃지가 우측 끝에 있는 밀도 높은 목록형 카드로 표시한다
- (UI) 하위 후보 요건 카드는 좌측 들여쓰기와 세로선으로 하위 관계가 구분된다
- (UI) 요건 검색/선택 대화상자는 요건 ID, 제목, 추적 상태, 카드 상태, 제품 영역, 우선순위, 명세 역할, 상위 요건 ID로 후보 요건을 검색할 수 있다
- (UI) 상위 요건이 검색어와 직접 일치하면 직계 하위 후보 요건도 목록에 표시되고 상위 검색 포함으로 구분된다
- (UI) 요건 검색/선택 대화상자는 별도 현재 선택 요약 영역 없이 후보 목록에서 선택된 요건을 구분하고 단일 요건 선택과 선택 해제를 지원한다
- (UI) 요건 검색/선택 대화상자는 검색 결과 수와 무관하게 viewport 기준 고정 높이를 유지하고 후보 목록 영역만 상하 스크롤된다
- (UI) 검색 결과가 없으면 빈 결과 안내를 표시한다
- (UI) Storybook Docs에서 요건 검색/선택 대화상자 story는 닫힌 호출 화면 프레임으로 렌더링되어 문서 본문을 가리지 않고 Canvas에서는 열린 대화상자 상태를 검토할 수 있다

## 검증 대상

- API: 불필요
- DB: 불필요
- UI: 필요
- Storybook: 필요
- E2E: 불필요
- STATIC: 불필요

## API Skeleton

- 해당 없음

## DB Skeleton

- 해당 없음

## UI Skeleton

- 화면 표면: `harness/ui/src/features/requirements/RequirementPickerDialog.tsx`를 공용 요건 선택 대화상자 컴포넌트로 둔다.
- 주요 영역: 대화상자 제목과 설명, 검색 입력, 검색 입력 안의 선택 해제 버튼, 상하 스크롤 가능한 후보 요건 목록형 카드, 빈 결과 안내를 둔다.
- 표시 필드: 요건 ID, 제목, 추적 상태, 명세 역할, 상위 요건 ID, 하위 요건 수, 후보 목록 안의 현재 선택 여부를 표시한다. 요건 ID와 제목은 한 줄로 배치하고, 추적 상태 뱃지는 카드 우측 끝에 둔다. 카드 상태, 제품 영역, 우선순위는 검색 대상에 남기되 후보 카드에는 표시하지 않는다.
- 구조 표시: 별도 구조 영역을 크게 두지 않고 카드 안의 역할/상위/하위 뱃지와 하위 후보 카드의 좌측 들여쓰기·세로선으로 표시한다. 전체 관련 요건 목록은 카드에 노출하지 않는다.
- 상태 목록: 기본 열림, Docs 닫힌 호출 화면 프레임, 상위/하위 요건 구조, 검색 결과, 상위 검색 하위 포함, 선택 요건 있음, 긴 후보 목록 스크롤, 빈 결과를 검토 상태로 둔다.
- 사용자 행위: 호출 화면에서 대화상자를 열고, 검색어를 입력하고, 후보 요건 하나를 선택하거나 현재 선택을 해제한다. 후보 목록의 출처와 선택값의 적용 방식은 호출 화면이 소유한다.

## Storybook 계약

- Harness/Requirements/RequirementPickerDialog: Open, Hierarchy, SearchResults, ParentSearchIncludesChildren, WithSelection, ScrollableList, EmptyResult

## 의사결정 로그

- 결정일: 2026-06-12
  결정: 요건 검색/선택 대화상자를 실행 화면과 Change Set 화면의 하위 동작이 아니라 별도 공용 UI 요건으로 분리한다.
  이유: 같은 대화상자가 여러 화면에서 재사용되며, 후보 표시·검색·단일 선택·선택 해제·빈 결과는 부모 화면과 독립적으로 검증해야 하는 공통 계약이다.
  결정자: REDSTONE
  영향: REQ-034와 REQ-035는 대화상자 내부 검색/선택 세부 동작을 REQ-037에 위임하고, 선택값을 각 화면에서 어떻게 사용하는지만 유지한다.

- 결정일: 2026-06-12
  결정: 후보 요건은 목록형 카드로 표시하고, 계층 정보는 명세 역할, 상위 요건 ID, 하위 요건 수로 축약한다.
  이유: 대화상자는 빠른 검색과 단일 선택이 목적이므로, 전체 관련 요건 목록보다 선택에 필요한 상위/하위 구조만 보여주는 편이 스캔성이 높다.
  결정자: REDSTONE
  영향: 하위 후보 요건은 좌측 들여쓰기와 세로선으로 구분하고, 상세한 관련 요건 검토는 호출 화면 또는 요건 상세 화면이 담당한다.

- 결정일: 2026-06-12
  결정: 대화상자 상단의 현재 선택 요약 영역을 제거하고, 후보 카드는 요건 ID와 제목을 한 줄에 배치하며 추적 상태 뱃지를 우측 끝에 둔다.
  이유: 이미 호출 화면에서 선택값의 업무 의미를 보여주고 있으므로 대화상자 내부의 선택 요약은 중복이며, 카드 상태·제품 영역·우선순위까지 카드에 표시하면 검색 결과 스캔성이 떨어진다.
  결정자: REDSTONE
  영향: 선택 해제는 검색 입력 안의 작은 해제 버튼으로 제공하고, 카드 상태·제품 영역·우선순위는 검색 대상에는 남지만 후보 카드 표시 필드에서는 제외한다. 긴 제목은 한 줄 안에서 말줄임 처리한다.

- 결정일: 2026-06-12
  결정: 상위 요건이 검색어와 직접 일치하면 직계 하위 후보 요건도 함께 표시하고 `상위 검색 포함`으로 구분한다.
  이유: 상위 요건은 기능 목표나 통합 여정이고 실제 선택·실행 대상은 하위 원자 요건인 경우가 많아, 상위 검색만으로 하위 후보를 찾을 수 있어야 한다.
  결정자: REDSTONE
  영향: 하위 요건이 검색어와 직접 일치한 경우에는 상위 요건을 자동 포함하지 않고, 일반 관련 요건과 재귀 하위는 확장하지 않는다.

- 결정일: 2026-06-12
  결정: 대화상자는 검색 결과 수와 무관하게 viewport 기준 고정 높이를 유지하고 후보 목록 영역만 상하 스크롤한다.
  이유: 검색어 입력으로 결과 수가 바뀔 때 대화상자 높이가 흔들리면 검색 입력과 후보 카드의 시각적 위치가 계속 달라져 선택 흐름을 방해한다.
  결정자: REDSTONE
  영향: Storybook에 `ScrollableList` 상태를 유지하고, 후보 목록 컨테이너는 대화상자 안에서 남는 고정 높이 영역을 사용해 빈 상태와 스크롤 상태를 처리한다.

## BDD 테스트 리뷰

- 시나리오 문서: `harness/docs/scenarios/REQ-037-harness-ui-requirement-picker-dialog.feature`

### 요건 Skeleton 설계 이력

- 설계일: 2026-06-12
  검증 설계: `.feature`의 6개 Scenario가 카드 수용 기준 8개를 `Covers:`로 연결한다. 모든 AC는 사용자가 관찰하는 대화상자 UI 결과이므로 `(UI)`로 검증한다.
  UI Skeleton: `RequirementPickerDialog`는 호출 화면이 전달한 `RequirementRow` 후보 목록을 기준으로 요건 ID와 제목을 한 줄에 표시하고, 추적 상태는 우측 끝 뱃지로, 명세 역할과 상위/하위 요건 구조는 같은 줄의 보조 뱃지로 축약 표시한다. 상위 검색 하위 포함, 검색, 단일 선택, 선택 해제, 검색 결과 수와 무관한 viewport 기준 고정 높이, 긴 후보 목록 스크롤, 빈 결과 상태를 제공한다. 현재 선택 요약은 두지 않고 선택 후보만 강조한다. 호출 화면별 제목·설명 문구는 props로 바꿀 수 있지만 후보 목록과 선택값의 의미 적용은 부모 화면이 소유한다.
  Storybook 계약: `Harness/Requirements/RequirementPickerDialog`의 `Open`, `Hierarchy`, `SearchResults`, `ParentSearchIncludesChildren`, `WithSelection`, `ScrollableList`, `EmptyResult` 상태가 있어야 한다. Docs에서는 열린 portal이 문서 본문을 가리지 않도록 닫힌 호출 화면 프레임으로 렌더링한다.
  서버 Skeleton: 해당 없음. 대화상자는 부모 화면 DTO의 요건 후보 목록을 받는다.
  추적 정책: `(UI)` AC는 harness/ui Storybook Vitest 결과로 판정한다.
  검증: Skeleton 설계 단계이므로 실행 테스트는 아직 작성하지 않는다. `npm run harness:trace -- --requirement REQ-037`로 카드/시나리오 정합성을 확인한다.
  Skeleton 결과: 승인 대기

### 테스트 리뷰

- 리뷰일: 2026-06-12
  리뷰자: REDSTONE
  확인: Skeleton 검토중 단계. UI Skeleton과 Storybook surface를 작성했고 실행 테스트는 아직 작성하지 않았다.
  결과: 미완료

## 열린 질문

- 없음
