# 요건 카드

요건 ID: REQ-003
제목: 할 일 카테고리 관리
우선순위: 중간
상태: 초안

## 사용자/목적

로그인 사용자는 자신의 할 일을 분류하기 위한 카테고리를 생성, 조회, 수정, 삭제할 수 있어야 한다. 또한 신규 사용자는 가입 직후 사용할 수 있는 시스템 기본 카테고리를 받는다.

## 범위

- 사용자는 이름, 색상, 설명, 정렬 순서를 입력해 카테고리를 생성한다.
- 이름은 앞뒤 공백을 제거한 뒤 저장하고 중복 판단에 사용한다.
- 정렬 순서를 입력하지 않으면 본인 카테고리의 최대값에 1024를 더한 값으로 자동 할당한다.
- 사용자는 자신의 카테고리 목록을 `displayOrder` 오름차순, 동률이면 `id` 오름차순으로 조회한다.
- 사용자는 자신의 카테고리 내용을 부분 수정한다. 요청에 포함된 필드만 변경하고 누락된 필드는 기존 값을 유지하며, `color` 또는 `description`을 명시적으로 `null`로 보내면 값을 비운다. 수정 시 포함된 필드는 생성과 동일한 검증 규칙(이름 trim·길이, 설명 길이, 색상 형식, 이름 중복)을 통과해야 한다.
- 사용자는 자신의 카테고리를 영구 삭제한다.
- 같은 사용자 안에서 카테고리 이름은 유일해야 한다.
- 신규 사용자가 가입할 때 시스템 기본 카테고리 3개가 자동 생성된다.
- 인증된 사용자는 요청 헤더 `X-User-Id`로 식별한다.

## 표준 용어

- category.category
- category.name
- category.color
- category.description
- category.displayOrder
- category.create
- category.list
- category.update
- category.delete
- category.notFound
- category.duplicateName
- category.default
- user.id

## 제외 범위

- 할 일과 카테고리의 연결 (REQ-002에서 다룬다)
- 다른 사용자와의 카테고리 공유
- 카테고리 정렬 정책의 자동 재배치 (예: 삭제 시 displayOrder gap 메우기)
- 색상에 대한 사용자 정의 팔레트 관리
- 시스템 기본 카테고리에 대한 별도 보호 (사용자는 기본 카테고리도 자유롭게 수정/삭제할 수 있다)
- 검색, 필터, 페이지네이션
- 정식 인증 방식 (세션, JWT 등)

## 확인 질문 로그

- 질문일: 2026-05-21
  질문: 카테고리에 어떤 속성을 둘 것인가?
  답변: 이름, 색상, 설명, 정렬 순서 네 가지를 둔다.
  반영: 범위, 표준 용어, 수용 기준에 반영했다.

- 질문일: 2026-05-21
  질문: 할 일과 카테고리의 관계는 어떻게 정의할 것인가?
  답변: 본 카드에서는 카테고리 자체만 다루고, 할 일에서 카테고리를 참조하는 동작은 REQ-002의 할 일 관리에서 정의한다.
  반영: 범위, 제외 범위, 의사결정 로그에 반영했다.

- 질문일: 2026-05-21
  질문: 참조 중인 카테고리 삭제 정책은 어떻게 할 것인가?
  답변: 할 일 연결을 본 카드에서 다루지 않으므로 별도 정책이 필요 없다. 본 카드에서는 단순 hard delete로 처리한다.
  반영: 범위, 의사결정 로그에 반영했다.

- 질문일: 2026-05-21
  질문: 카테고리 이름의 유일성과 기본 카테고리 정책은?
  답변: 같은 사용자 안에서 이름 유일, 가입 시 시스템 기본 카테고리 자동 생성, 사용자는 기본 카테고리를 포함해 모두 자유롭게 수정 또는 삭제할 수 있다.
  반영: 범위, 수용 기준, 의사결정 로그에 반영했다.

- 질문일: 2026-05-21
  질문: 시스템 기본 카테고리 시드의 구체적 구성은?
  답변: `업무`(#2563EB, displayOrder=1024), `개인`(#16A34A, displayOrder=2048), `기타`(#6B7280, displayOrder=3072) 3개를 생성한다.
  반영: 의사결정 로그와 수용 기준에 반영했다.

- 질문일: 2026-05-21
  질문: 이름의 trim과 공백만 입력 처리 정책은?
  답변: 서버에서 앞뒤 공백을 trim한 뒤 저장하고 중복 판단에 사용한다. trim 후 빈 문자열이면 400으로 거절한다.
  반영: 범위, 수용 기준, 의사결정 로그에 반영했다.

- 질문일: 2026-05-21
  질문: displayOrder 미입력 시 처리 방식은?
  답변: 선택 입력으로 두고, 미입력 시 본인 카테고리의 최대 displayOrder에 1024를 더한 값으로 자동 할당한다. 카테고리가 없으면 1024를 부여한다.
  반영: 범위, 수용 기준, 의사결정 로그에 반영했다.

- 질문일: 2026-05-21
  질문: 수정 의미론과 목록 정렬은?
  답변: 부분 수정(PATCH)으로 정의하고 누락 필드는 유지, 명시적 `null`은 값을 비운다. 목록은 `displayOrder` 오름차순, 동률 시 `id` 오름차순으로 반환한다.
  반영: 범위, 수용 기준, 의사결정 로그에 반영했다.

- 질문일: 2026-05-21
  질문: 길이 제한과 HTTP 상태 코드 정책은?
  답변: 이름 최대 50자, 설명 최대 500자. 생성 201, 검증 실패 400, 중복 409, 조회/수정 성공 200, 삭제 성공 204, 존재하지 않거나 타인 자원 404.
  반영: 의사결정 로그와 수용 기준에 반영했다.

## 수용 기준

- 유효한 정보이면 카테고리가 생성된다
- 이름은 앞뒤 공백이 제거되어 저장된다
- 이름이 비어 있거나 공백만 입력하면 카테고리 생성이 거절된다
- 이름이 50자를 초과하면 카테고리 생성이 거절된다
- 설명이 500자를 초과하면 카테고리 생성이 거절된다
- 색상 형식이 잘못되면 카테고리 생성이 거절된다
- 같은 사용자가 이미 등록한 이름이면 카테고리 생성이 거절된다
- 정렬 순서를 입력하지 않으면 본인 카테고리의 최대 정렬 순서에 1024를 더한 값으로 할당된다
- 본인의 카테고리 목록이 정렬 순서 오름차순, 동률이면 식별자 오름차순으로 조회된다
- 신규 사용자가 가입하면 업무(#2563EB, 1024), 개인(#16A34A, 2048), 기타(#6B7280, 3072) 세 개의 기본 카테고리가 자동 생성된다
- 수정 요청에 포함된 필드만 변경되고 누락된 필드는 기존 값을 유지한다
- 수정 요청에서 색상에 null을 명시하면 색상이 비워진다
- 수정 요청에서 설명에 null을 명시하면 설명이 비워진다
- 수정 시 이름이 비어 있거나 공백만 입력하면 수정이 거절된다
- 수정 시 이름이 50자를 초과하면 수정이 거절된다
- 수정 시 설명이 500자를 초과하면 수정이 거절된다
- 수정 시 색상 형식이 잘못되면 수정이 거절된다
- 수정 시 같은 사용자 안에서 이미 사용 중인 이름으로 바꾸려 하면 수정이 거절된다
- 존재하지 않는 카테고리를 수정하려 하면 거절된다
- 다른 사용자의 카테고리를 수정하려 하면 거절된다
- 본인의 카테고리를 영구 삭제할 수 있다
- 존재하지 않는 카테고리를 삭제하려 하면 거절된다
- 다른 사용자의 카테고리를 삭제하려 하면 거절된다

## 의사결정 로그

- 결정일: 2026-05-21
  결정: 본 카드는 카테고리 리소스(이름, 색상, 설명, 정렬 순서)의 CRUD와 가입 시 기본 시드만 다루고, 할 일에서 카테고리를 참조하는 동작(연결, 응답 표현, 변경, 제거)은 REQ-002에서 정의한다.
  이유: 카드를 5-15분 안에 검토할 수 있는 크기로 유지하고, 카테고리 자체와 사용 시 동작을 책임이 다른 두 카드로 분리한다.
  결정자: Product Owner
  영향: 본 카드의 카테고리 엔티티 자체는 할 일 참조를 두지 않는다. 단, 카테고리 삭제 시 연결된 할 일에 미치는 영향은 본 카드의 책임이며 아래 "카테고리 삭제 시 SET NULL" 의사결정에서 정의한다.

- 결정일: 2026-05-21
  결정: 같은 사용자 안에서 카테고리 이름은 유일하게 유지한다. 다른 사용자는 동일한 이름을 가질 수 있다.
  이유: 사용자별 카테고리 목록의 일관성을 보장하면서 사용자 사이의 격리를 유지한다.
  결정자: Product Owner
  영향: DB 제약은 (`user_id`, `name`) 복합 unique로 정의한다. 중복 검출 시 `DuplicateCategoryNameException`을 던지고 409로 응답한다.

- 결정일: 2026-05-21
  결정: 신규 사용자 가입 시 시스템 기본 카테고리를 자동 생성한다.
  이유: 가입 직후 빈 카테고리 상태에서 할 일 분류 흐름을 막지 않기 위함이다.
  결정자: Product Owner
  영향: REQ-001의 회원 가입 흐름은 가입 성공 후 기본 카테고리 시드를 함께 생성한다. 시드의 구체 구성은 아래 "기본 카테고리 시드" 의사결정 로그에서 확정한다.

- 결정일: 2026-05-21
  결정: 사용자는 시스템 기본 카테고리도 일반 카테고리와 동일하게 수정 또는 삭제할 수 있다.
  이유: 기본 카테고리에만 별도 보호 규칙을 두면 정책 분기가 늘어나고 사용자가 분류 체계를 자기 맥락에 맞게 정리하지 못한다.
  결정자: Product Owner
  영향: Entity에 system_default flag를 두지 않거나 두더라도 정책상으로는 일반 카테고리와 동일하게 다룬다.

- 결정일: 2026-05-21
  결정: 색상 값은 `#RRGGBB` 형식의 hex 문자열로 제한한다. 색상 미입력은 허용한다.
  이유: 형식이 명확해 검증이 단순하고, 클라이언트가 자유롭게 사용자 지정 색상을 선택할 수 있다.
  결정자: Product Owner
  영향: DTO는 정규식 검증을 적용하고, 색상 미입력 시 null로 저장한다.

- 결정일: 2026-05-21
  결정: 다른 사용자의 카테고리에 대한 접근은 존재 여부를 노출하지 않기 위해 404로 동일하게 응답한다.
  이유: REQ-002와 동일한 정책을 적용해 다른 사용자 자원의 존재 여부 노출을 막는다.
  결정자: Tech Lead
  영향: 수용 기준 중 "다른 사용자의 카테고리" 케이스는 BDD 테스트에서 동일한 404 응답을 검증한다.

- 결정일: 2026-05-21
  결정: 시스템 기본 카테고리 시드는 `업무`(#2563EB, displayOrder=1024), `개인`(#16A34A, displayOrder=2048), `기타`(#6B7280, displayOrder=3072) 3개로 고정한다.
  이유: 가입 직후의 정리 부담을 늘리지 않으면서 일반적인 분류 흐름을 커버하고, 테스트와 UX 양쪽에서 정확한 어셔션이 가능하다.
  결정자: Product Owner
  영향: REQ-001의 회원 가입 흐름에서 이 세 카테고리를 동일 트랜잭션으로 시드한다. 시드는 일반 카테고리와 동일한 권한 정책을 따른다(수정·삭제 자유).

- 결정일: 2026-05-21
  결정: 이름은 서버에서 앞뒤 공백을 trim한 뒤 저장하고, 중복 판정도 trim된 이름 기준으로 수행한다. trim 후 빈 문자열이면 400으로 거절한다.
  이유: `"  업무  "`와 `"업무"`가 서로 다른 카테고리로 보이는 사용자 혼란을 막고, 공백만 입력하는 사실상 빈 이름을 같은 검증 분기에서 처리한다.
  결정자: Product Owner, Tech Lead
  영향: DTO의 `name`은 검증 전 trim된다. `DuplicateCategoryNameException` 판정도 trim된 값으로 한다.

- 결정일: 2026-05-21
  결정: displayOrder는 선택 입력이며 미입력 시 본인 카테고리의 최대 displayOrder에 1024를 더한 값으로 자동 할당한다. 본인 카테고리가 없으면 1024를 부여한다.
  이유: 기본 시드와 신규 생성 규칙이 일관되며, 1024 단위 간격은 중간 삽입을 위한 여지를 남기면서 자동 재배치 정책을 도입하지 않아도 된다.
  결정자: Product Owner, Tech Lead
  영향: Category 도메인 서비스는 생성 시 displayOrder 누락 여부를 확인하고 자동 할당한다. 충돌이 발생해도 (`displayOrder`, `id`) 안정 정렬로 흔들리지 않는다.

- 결정일: 2026-05-21
  결정: 카테고리 수정은 부분 수정(PATCH 의미)으로 정의한다. 요청 본문에 없는 필드는 기존 값을 유지하고, `color`와 `description`은 명시적으로 `null`을 보내면 값을 비운다.
  이유: 카테고리 관리 화면에서 이름만 또는 색상만 바꾸는 흐름이 자연스럽고, 테스트도 변경 의도를 정확히 표현할 수 있다.
  결정자: Product Owner
  영향: API 관찰 동작은 본 카드 수용 기준으로 확정되었다(필드 누락 시 유지, 명시적 null이면 비우기). 이 의미론을 표현하는 DTO/JSON 구체 방식(예: `JsonNullable`, 별도 sentinel, custom deserializer 중 선택)은 구현 단계에서 결정한다.

- 결정일: 2026-05-21
  결정: 카테고리 목록은 `displayOrder` 오름차순, 동률 시 `id` 오름차순으로 반환한다.
  이유: 사용자가 지정한 정렬 의도를 우선하고, 동률 상황에서도 결정적 정렬이 유지되어 테스트가 흔들리지 않는다.
  결정자: Product Owner, Tech Lead
  영향: `ListCategoriesResponse`의 순서는 위 규칙으로 고정한다. 클라이언트는 반환 순서를 신뢰할 수 있다.

- 결정일: 2026-05-21
  결정: 이름 최대 50자, 설명 최대 500자. HTTP 상태 코드는 생성 201, 검증 실패 400, 중복 409, 조회·수정 성공 200, 삭제 성공 204, 존재하지 않거나 타인 자원 접근은 404.
  이유: 일반적인 RESTful 관습을 따르고, 상태 코드 기반 BDD 어셔션을 결정적으로 만든다.
  결정자: Tech Lead
  영향: DTO는 길이 제약을 Bean Validation으로 표현하고, `common.apiError` 응답 구조에 맞춰 검증 실패를 표준화한다.

- 결정일: 2026-05-21
  결정: 수정 요청 본문에 포함된 모든 필드는 생성 시와 동일한 검증 규칙(이름 trim 후 빈 문자열 거절, 이름 50자, 설명 500자, 색상 #RRGGBB 형식, 이름 중복 거절)을 적용한다.
  이유: 부분 수정이라도 저장 상태가 항상 검증 규칙을 만족해야 하므로 생성과 수정의 검증을 동일하게 두는 편이 일관적이다.
  결정자: Product Owner, Tech Lead
  영향: 검증 실패 시 동일한 400 또는 409 응답을 반환하고, BDD 테스트는 생성 검증과 수정 검증을 각각 분리해 어셔션한다.

- 결정일: 2026-05-21
  결정: 존재하지 않는 카테고리에 대한 삭제 요청도 수정과 동일하게 404로 응답한다.
  이유: 동일 자원에 대한 부재/타인 접근 정책을 통일해야 클라이언트가 일관된 오류 처리를 할 수 있고, BDD 어셔션도 단순해진다.
  결정자: Tech Lead
  영향: 삭제 핸들러는 부재와 타인 자원 접근을 동일한 `CategoryNotFoundException` 분기로 처리한다.

- 결정일: 2026-05-21
  결정: 카테고리를 삭제하면 해당 카테고리에 연결된 본인 할 일의 `categoryId`를 `null`로 설정(SET NULL)한다. 할 일 자체는 유지된다.
  이유: 사용자가 카테고리 정리만으로 할 일 데이터를 잃지 않게 하고, 거절(409)로 인한 선처리 부담이나 cascade로 인한 데이터 손실 위험을 모두 피한다.
  결정자: Product Owner, Tech Lead
  영향: 카테고리 삭제 핸들러는 같은 트랜잭션에서 본인 할 일의 `categoryId`를 `null`로 갱신한다. 본 동작의 사용자 관점 검증(할 일 유지 + 분류 해제)은 REQ-002에서 수용 기준으로 정의된다. 본 카드는 정책 책임만 가지며, 통합 테스트는 REQ-002 Todo Entity가 도입된 뒤 함께 추가한다.

## BDD 테스트 리뷰

- 리뷰일: 2026-05-21
  리뷰자: Product Owner, Tech Lead
  확인: 수용 기준 23개가 5개 Acceptance Test 클래스(Create 8, List 1, DefaultSeed 1, Update 10, Delete 3)의 `@Covers`/`@DisplayName`과 정확히 일치한다. 정상/검증/중복/소유권/시드/PATCH 의미론(누락 유지·명시적 null 비우기)까지 어셔션이 분리되어 있고, `traceRequirements` 결과 모든 AC가 PASS로 매핑되어 REQ-003은 GREEN 상태이다.
  결과: 승인

## 열린 질문

- 없음
