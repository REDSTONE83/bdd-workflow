# 요건 카드

요건 ID: REQ-002
제목: 개인별 할 일 관리
우선순위: 높음
상태: 초안

## 사용자/목적

로그인 사용자는 자신의 할 일을 생성, 조회, 수정, 완료 처리, 삭제할 수 있어야 한다.

## 범위

- 사용자는 제목, 설명, 마감일, 우선순위, 카테고리 ID를 입력해 할 일을 생성한다. 카테고리 ID는 선택 입력이며 미입력 시 미분류 상태로 저장된다.
- 제목은 앞뒤 공백을 제거한 뒤 저장하며, trim 후 빈 문자열이면 거절한다. 제목은 최대 100자, 설명은 최대 1000자이다.
- 마감일은 ISO 8601 날짜(`YYYY-MM-DD`)로 입력한다. 시각은 포함하지 않으며 선택 입력이고 과거 날짜도 허용한다.
- 우선순위는 `HIGH`, `MEDIUM`, `LOW` 중 하나이며 미입력 시 `MEDIUM`이 적용된다.
- 사용자는 자신의 할 일 목록을 조회한다. 목록은 미완료(`completed=false`)가 먼저, 같은 상태에서는 우선순위 내림차순(HIGH→MEDIUM→LOW), 동률은 식별자 오름차순으로 정렬되어 반환된다. 응답에는 연결된 카테고리의 ID, 이름, 색상이 함께 포함되며 카테고리가 없으면 카테고리 정보는 `null`이다.
- 사용자는 자신의 할 일 내용을 부분 수정한다(PATCH 의미). 누락된 필드는 기존 값을 유지하고, `description`/`dueDate`/`categoryId`는 명시적 `null`로 비울 수 있다. `title`, `priority`, `completed`는 명시적 `null`을 허용하지 않는다.
- 사용자는 PATCH의 `completed` 필드(`true`/`false`)로 완료 처리하거나 미완료로 되돌린다.
- 사용자는 자신의 할 일을 영구 삭제한다.
- 인증된 사용자는 요청 헤더 `X-User-Id`로 식별한다.

## 표준 용어

- todo.task
- todo.title
- todo.description
- todo.dueDate
- todo.priority
- todo.completed
- todo.create
- todo.list
- todo.update
- todo.complete
- todo.delete
- todo.notFound
- todo.invalidCategory
- category.category
- category.id
- category.name
- category.color
- user.id

## 제외 범위

- 카테고리 자체의 CRUD (REQ-003에서 다룬다. 본 카드는 할 일이 카테고리를 참조하는 동작만 다룬다.)
- 세션, JWT 등 정식 인증 방식
- 반복 일정, 알림, 첨부 파일
- 마감일을 과거 날짜로 입력하는 것에 대한 차단
- 검색, 정렬, 필터, 페이지네이션
- 다른 사용자와의 공유 또는 위임

## 확인 질문 로그

- 질문일: 2026-05-21
  질문: 이번 요건의 핵심 기능 범위는 어디까지인가?
  답변: 할 일 생성, 목록 조회, 완료 처리, 수정 및 삭제. 카테고리 관리는 별도 카드로 분리한다.
  반영: 범위와 제외 범위에 반영했다.

- 질문일: 2026-05-21
  질문: 할 일에 어떤 속성을 둘 것인가?
  답변: 제목, 설명, 마감일, 우선순위 네 가지를 둔다.
  반영: 범위와 표준 용어, 수용 기준에 반영했다.

- 질문일: 2026-05-21
  질문: 사용자 인증과 소유권 정책은 어떻게 다룰 것인가?
  답변: 인증된 사용자 본인의 데이터만 다룬다. 본 카드에서는 `X-User-Id` 헤더로 사용자 ID를 받고, 정식 인증은 별도 요건으로 분리한다.
  반영: 범위, 제외 범위, 의사결정 로그에 반영했다.

- 질문일: 2026-05-21
  질문: 카테고리는 별도 리소스로 둘지, 할 일의 단순 라벨로 둘지?
  답변: 별도 리소스로 두고 CRUD를 제공한다. 본 카드(REQ-002)와 분리해 REQ-003에서 처리한다.
  반영: 제외 범위와 의사결정 로그에 반영했다.

- 질문일: 2026-05-21
  질문: 우선순위 단계와 완료 상태 정책은?
  답변: 우선순위는 HIGH, MEDIUM, LOW 세 단계로 두고 미지정 시 MEDIUM. 완료 상태는 양방향 토글을 허용하고, 마감일은 과거 날짜도 허용하며, 삭제는 hard delete로 처리한다.
  반영: 수용 기준과 의사결정 로그에 반영했다.

- 질문일: 2026-05-21
  질문: 할 일 생성 시 `categoryId`는 필수인가 선택인가?
  답변: 선택(nullable). 미입력 시 미분류 상태로 저장된다.
  반영: 범위, 수용 기준, 의사결정 로그에 반영했다.

- 질문일: 2026-05-21
  질문: 존재하지 않거나 다른 사용자의 카테고리 ID를 주면 어떻게 응답하는가?
  답변: 부재·타인 자원 모두 400 Bad Request로 응답하고 `code=INVALID_CATEGORY`, `field=categoryId`로 통일한다.
  반영: 수용 기준, 의사결정 로그에 반영했다.

- 질문일: 2026-05-21
  질문: 할 일 응답에 카테고리 정보는 어떻게 표현하는가?
  답변: `categoryId`, 이름, 색상을 nested 객체(`category: {id, name, color}`)로 포함한다. 연결이 없으면 `category`는 `null`이다.
  반영: 범위, 수용 기준, 의사결정 로그에 반영했다.

- 질문일: 2026-05-21
  질문: 할 일 수정에서 카테고리 변경과 제거는 어떻게 표현하는가?
  답변: REQ-003의 PATCH 의미론과 동일하게 처리한다. 누락은 변경 없음, 명시적 `null`은 연결 해제, 유효한 ID는 카테고리 변경이다.
  반영: 범위, 수용 기준, 의사결정 로그에 반영했다.

- 질문일: 2026-05-21
  질문: 연결된 할 일이 있는 카테고리를 삭제할 때 어떻게 처리하는가?
  답변: 카테고리 삭제 시 연결된 할 일의 `categoryId`를 `null`로 설정(SET NULL)한다. 할 일 자체는 유지된다. 이 정책은 REQ-003의 의사결정 로그에도 함께 보강한다.
  반영: 수용 기준, 의사결정 로그에 반영했다. REQ-003 카드에도 동일 정책을 추가한다.

- 질문일: 2026-05-21
  질문: 제목과 설명의 최대 길이는 얼마로 두는가?
  답변: 제목 100자, 설명 1000자. 모두 trim 후 기준이다.
  반영: 범위, 수용 기준, 의사결정 로그에 반영했다.

- 질문일: 2026-05-21
  질문: 마감일에 시각을 포함하는가?
  답변: 날짜만(`LocalDate`, `YYYY-MM-DD`) 포함한다. 시각은 다루지 않는다.
  반영: 범위, 수용 기준, 의사결정 로그에 반영했다.

- 질문일: 2026-05-21
  질문: 할 일 목록의 정렬 정책은?
  답변: `completed` 오름차순 → `priority` 내림차순(HIGH→MEDIUM→LOW) → `id` 오름차순.
  반영: 범위, 수용 기준, 의사결정 로그에 반영했다.

- 질문일: 2026-05-21
  질문: 완료 토글은 별도 엔드포인트로 둘지 PATCH 필드로 둘지?
  답변: PATCH의 `completed` 필드(`true`/`false`)로 처리한다. 별도 엔드포인트는 두지 않는다.
  반영: 범위, 수용 기준, 의사결정 로그에 반영했다.

- 질문일: 2026-05-21
  질문: 수정의 PATCH 의미론과 검증 규칙은 어떻게 정의하는가?
  답변: 누락 필드는 유지하고, `description`/`dueDate`/`categoryId`만 명시적 `null`로 비울 수 있다. `title`/`priority`/`completed`는 `null` 거절. 모든 필드는 생성과 동일한 검증 규칙(trim, 길이, enum, ISO 날짜 형식, 카테고리 유효성)을 적용한다.
  반영: 범위, 수용 기준, 의사결정 로그에 반영했다.

## 수용 기준

- 유효한 정보이면 할 일이 생성된다
- 제목은 앞뒤 공백이 제거되어 저장된다
- 제목이 비어 있거나 공백만 입력하면 할 일 생성이 거절된다
- 제목이 100자를 초과하면 할 일 생성이 거절된다
- 설명이 1000자를 초과하면 할 일 생성이 거절된다
- 마감일 형식이 ISO 8601 날짜가 아니면 할 일 생성이 거절된다
- 우선순위가 HIGH, MEDIUM, LOW 중 하나가 아니면 할 일 생성이 거절된다
- 우선순위를 입력하지 않으면 기본 우선순위 MEDIUM으로 할 일이 생성된다
- 할 일 생성 시 카테고리 ID를 입력하지 않으면 미분류 상태로 저장된다
- 할 일 생성 시 본인의 카테고리 ID를 명시하면 해당 카테고리에 연결되어 저장된다
- 할 일 생성 시 존재하지 않거나 다른 사용자의 카테고리 ID를 명시하면 거절된다
- 본인의 할 일 목록만 조회된다
- 본인의 할 일 목록은 미완료가 먼저, 같은 상태에서는 우선순위 내림차순, 동률은 식별자 오름차순으로 정렬되어 반환된다
- 할 일 응답에는 연결된 카테고리의 ID, 이름, 색상이 함께 반환되며, 연결이 없으면 카테고리 정보는 null이다
- 수정 요청에 포함된 필드만 변경되고 누락된 필드는 기존 값을 유지한다
- 수정 요청에서 설명에 null을 명시하면 설명이 비워진다
- 수정 요청에서 마감일에 null을 명시하면 마감일이 비워진다
- 수정 요청에서 카테고리 ID에 null을 명시하면 카테고리 연결이 해제된다
- 수정 시 제목이 비어 있거나 공백만 입력하면 수정이 거절된다
- 수정 시 제목이 100자를 초과하면 수정이 거절된다
- 수정 시 설명이 1000자를 초과하면 수정이 거절된다
- 수정 시 마감일 형식이 ISO 8601 날짜가 아니면 수정이 거절된다
- 수정 시 우선순위가 HIGH, MEDIUM, LOW 중 하나가 아니면 수정이 거절된다
- 수정 시 본인의 카테고리 ID를 명시하면 해당 카테고리로 연결이 변경된다
- 수정 시 존재하지 않거나 다른 사용자의 카테고리 ID를 명시하면 거절된다
- 수정 시 completed에 true를 명시하면 할 일이 완료 상태로 바뀐다
- 수정 시 completed에 false를 명시하면 할 일이 미완료 상태로 되돌아간다
- 존재하지 않는 할 일을 수정하려 하면 거절된다
- 다른 사용자의 할 일을 수정하려 하면 거절된다
- 연결된 카테고리가 삭제되면 본인의 할 일은 유지되고 카테고리 연결만 해제된다
- 본인의 할 일을 영구 삭제할 수 있다
- 존재하지 않는 할 일을 삭제하려 하면 거절된다
- 다른 사용자의 할 일을 삭제하려 하면 거절된다

## 의사결정 로그

- 결정일: 2026-05-21
  결정: 카테고리 자체의 CRUD(생성·수정·삭제·기본 시드)는 REQ-003에서 다루고, 할 일이 카테고리를 참조하는 동작(연결, 응답 표현, 변경, 제거)은 본 카드에서 다룬다.
  이유: 카테고리 리소스와 그 사용 시 동작을 책임이 다른 두 카드로 분리해 각 카드가 5-15분 안에 검토 가능한 크기를 유지한다.
  결정자: Product Owner
  영향: 본 카드의 Todo Entity는 nullable `category_id` 외래키를 둔다. 카테고리 자원의 변경 정책은 REQ-003에서 정의하고, 본 카드는 그 정책을 따른다.

- 결정일: 2026-05-21
  결정: 인증 방식은 임시로 `X-User-Id` 요청 헤더로 받는다.
  이유: 본 카드의 목적은 할 일 관리 API와 BDD 테스트 연결을 보여주는 것이며, 정식 인증을 함께 처리하면 범위가 과도하게 커진다.
  결정자: Product Owner, Tech Lead
  영향: 세션이나 JWT 검증은 별도 요건으로 분리한다. 헤더가 없거나 형식이 잘못된 요청은 본 카드의 검증 범위에서 제외한다.

- 결정일: 2026-05-21
  결정: 다른 사용자의 할 일에 대한 접근은 존재 여부를 노출하지 않기 위해 404로 동일하게 응답한다.
  이유: 권한 부재(403)와 부존재(404)를 구분하면 다른 사용자의 자원 존재 여부가 노출된다.
  결정자: Tech Lead
  영향: 수용 기준 중 "다른 사용자의 할 일" 케이스는 BDD 테스트에서 동일한 404 응답을 검증한다.

- 결정일: 2026-05-21
  결정: 우선순위 enum은 HIGH, MEDIUM, LOW 세 단계로 두고 미지정 시 MEDIUM을 기본값으로 한다.
  이유: 세 단계가 일반적인 작업 관리 도구의 기본 표현과 일치하며 정렬 규칙을 단순하게 유지한다.
  결정자: Product Owner
  영향: DTO/Entity의 priority 필드는 enum 타입으로 정의하고 기본값을 MEDIUM으로 둔다.

- 결정일: 2026-05-21
  결정: 완료 상태는 boolean `completed` 한 필드로 표현하고 양방향 토글을 허용한다.
  이유: 상태 전이 도식이 단순하고, "다시 미완료로 되돌린다"는 사용자 요구를 직관적으로 지원한다.
  결정자: Product Owner, Tech Lead
  영향: 별도 상태 enum을 두지 않으며, 완료 처리 API는 한 엔드포인트로 양방향 토글을 모두 처리한다.

- 결정일: 2026-05-21
  결정: 삭제는 hard delete로 처리하고, 마감일은 과거 날짜도 허용한다.
  이유: 본 카드의 범위에서 복구나 감사 추적은 필요 없으며, 과거 마감일도 사용자가 기록 목적으로 입력할 수 있다.
  결정자: Product Owner
  영향: Entity는 soft delete 컬럼을 두지 않는다. 마감일 검증은 형식 검증만 수행한다.

- 결정일: 2026-05-21
  결정: `categoryId`는 할 일 생성·수정에서 선택 입력(nullable)으로 둔다. 미입력 시 미분류 상태로 저장된다.
  이유: 가입 시 기본 카테고리가 시드되지만 모든 할 일에 분류를 강제하지 않는다. 분류 부담을 사용자 선택에 맡긴다.
  결정자: Product Owner
  영향: Todo DTO와 Entity의 `categoryId`는 nullable로 둔다. 미분류 할 일은 응답의 `category` 필드가 `null`이다.

- 결정일: 2026-05-21
  결정: 존재하지 않거나 다른 사용자의 카테고리 ID를 명시하면 400 Bad Request로 거절하고 `code=INVALID_CATEGORY`, `field=categoryId`로 응답한다.
  이유: 카테고리 자원에 대한 GET이 아니라 할 일 요청의 입력 검증 실패다. 부재와 타인 자원을 동일 응답으로 묶어 존재 여부 노출을 막는다.
  결정자: Product Owner, Tech Lead
  영향: `TodoService`는 카테고리 ID 유효성을 본인 소유 카테고리 존재 여부로 확인한다. 검증 실패 시 별도 예외(`InvalidCategoryException`)로 400을 반환한다.

- 결정일: 2026-05-21
  결정: 할 일 응답에는 연결된 카테고리를 nested 객체 `category: {id, name, color}`로 포함한다. 연결이 없으면 `category`는 `null`이다.
  이유: UI가 별도 조회 없이 카테고리를 표시할 수 있고, 카테고리 변경이 즉시 응답에 반영된다.
  결정자: Product Owner
  영향: `TodoResponse`는 `categoryId` 대신 `category` nested 객체를 둔다. 응답 빌더는 카테고리를 조회해 함께 반환한다.

- 결정일: 2026-05-21
  결정: 할 일 수정은 부분 수정(PATCH 의미)으로 정의하고, REQ-003과 동일한 누락·null 처리 규칙을 적용한다. `categoryId`는 누락하면 변경 없음, 명시적 `null`이면 연결 해제, 유효 ID면 카테고리 변경이다.
  이유: REQ-003의 PATCH 정책과 통일해 사용자 관점의 일관성을 유지한다.
  결정자: Product Owner, Tech Lead
  영향: `UpdateTodoRequest`도 ObjectNode 기반으로 처리해 "필드 미포함"과 "명시적 null"을 구분한다. JSON 표현의 구체 방식은 REQ-003과 동일하게 구현 단계에서 정한다.

- 결정일: 2026-05-21
  결정: 카테고리 삭제 시 연결된 본인 할 일의 `categoryId`를 `null`로 설정(SET NULL)한다. 할 일 자체는 유지된다.
  이유: 사용자가 카테고리 정리만으로 할 일 데이터를 잃지 않게 하고, 거절(409)로 인한 선처리 부담도 없앤다.
  결정자: Product Owner, Tech Lead
  영향: 본 카드는 카테고리 삭제 후 자신의 할 일이 유지되고 분류만 해제되는 동작을 수용 기준으로 검증한다. 이 동작을 만드는 책임은 REQ-003의 카테고리 삭제 핸들러에 있으며, REQ-003 의사결정 로그에도 동일 정책을 함께 보강한다.

- 결정일: 2026-05-21
  결정: 제목은 trim 후 최대 100자, 설명은 최대 1000자로 제한한다. 제목은 trim 후 빈 문자열이면 거절한다.
  이유: 할 일은 한 줄 요약이 자연스러우므로 100자가 충분하고, 설명은 메모/상세 내용이 들어갈 수 있어 1000자로 두어 일반적인 사용 흐름을 막지 않는다.
  결정자: Product Owner
  영향: DTO/Entity의 title은 length 100, description은 length 1000으로 제약한다. 검증은 trim 후 길이를 기준으로 한다.

- 결정일: 2026-05-21
  결정: 마감일은 ISO 8601 날짜(`YYYY-MM-DD`)로만 표현한다. 시각은 포함하지 않으며 nullable이고 과거 날짜도 허용한다.
  이유: 할 일 관리의 UX는 보통 날짜 단위 사고이며, 시각까지 다루면 스케줄 도구의 영역으로 범위가 늘어난다.
  결정자: Product Owner
  영향: DTO/Entity의 `dueDate` 타입은 `LocalDate`로 둔다. 형식 위반은 400 `VALIDATION_FAILED`로 응답한다.

- 결정일: 2026-05-21
  결정: 할 일 목록 정렬은 `completed` 오름차순(미완료 먼저) → `priority` 내림차순(HIGH→MEDIUM→LOW) → `id` 오름차순으로 고정한다.
  이유: 미완료 작업을 우선적으로 노출하는 일반적 UX와 같은 상태 내에서 급한 일을 위로 두는 사용자 의도를 반영한다. 동률 시 식별자 정렬로 결정적 순서를 보장해 테스트가 흔들리지 않는다.
  결정자: Product Owner, Tech Lead
  영향: `ListTodosResponse`의 정렬은 위 규칙으로 고정한다. 클라이언트가 별도 정렬 옵션을 요구하면 후속 카드에서 보강한다.

- 결정일: 2026-05-21
  결정: 완료 토글은 별도 엔드포인트를 두지 않고 PATCH의 `completed` 필드(`true`/`false`)로 처리한다. `completed`에 `null` 또는 누락은 변경 없음으로 본다.
  이유: REQ-003 PATCH 의미론과 통일해 API 표면을 줄이고 사용자가 한 요청으로 여러 필드를 함께 변경할 수 있다.
  결정자: Product Owner, Tech Lead
  영향: `UpdateTodoRequest`(ObjectNode 기반)의 `completed` 필드 처리 분기를 PATCH 핸들러에 포함한다. 완료/미완료 전환은 PATCH 핸들러 안에서 검증과 함께 일관 처리된다.

- 결정일: 2026-05-21
  결정: 수정은 PATCH 의미론으로 정의한다. 누락 필드는 유지하고, `description`/`dueDate`/`categoryId`만 명시적 `null`로 비울 수 있다. `title`/`priority`/`completed`는 명시적 `null`을 허용하지 않는다. 수정 시 모든 필드는 생성과 동일한 검증 규칙(trim, 길이, enum, ISO 날짜 형식, 카테고리 유효성)을 통과해야 한다.
  이유: REQ-003 PATCH 정책과 통일하고, 수정 후에도 저장 상태가 항상 검증 규칙을 만족해야 한다.
  결정자: Product Owner, Tech Lead
  영향: PATCH 요청은 `ObjectNode` 기반으로 처리해 "필드 미포함"과 "명시적 null"을 구분한다. 비허용 필드에 `null`이 오면 400 `VALIDATION_FAILED`로 거절한다.

## BDD 테스트 리뷰

- 미완료

## 열린 질문

- 없음
